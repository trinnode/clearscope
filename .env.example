# ClearScope environment template
# Copy to .env.local for local development. All values are optional.

# Port the Next.js server listens on (default 3000)
# PORT=3000

# Midnight network to probe for status checks:
# undeployed | preview | preprod | mainnet
# CLEARSCOPE_NETWORK=undeployed